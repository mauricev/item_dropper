part of '../../item_dropper_multi_select.dart';

// Build methods
extension _MultiItemDropperStateBuilders<T> on _MultiItemDropperState<T> {
  // Build methods will be moved here
  // Examples:
  // - Widget build(BuildContext context) { ... }
  // - Widget _buildInputField() { ... }
  // - Widget _buildChip(...) { ... }
  // - Widget _buildTextFieldChip(...) { ... }
  // - Widget _buildDropdownOverlay() { ... }
  // - Widget _buildMaxReachedOverlay(...) { ... }
  // - Widget _buildEmptyStateOverlay(...) { ... }
  // - etc.
}

